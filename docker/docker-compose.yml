services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: course_simulator_backend
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=8000
    ports:
      - "8000:8000"
    restart: unless-stopped
